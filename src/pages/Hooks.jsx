import Buttons from "../components/Buttons";

export default function Hooks() {
  return (
    <div style={{ width: "100%", padding: "20px" }}>
      <Buttons prev={"/builders"} next={"/react-hook-form"} />
      <h1 style={{ margin: "0 auto" }}>
        ХУКИ (useId, useReducer, useTransition, useOptimistic)
      </h1>
      <section>
        <h2>useReducer </h2>
        <p>
          это хук, который работает похожим образом на Redux, но прямо внутри
          твоего компонента. Вместо того чтобы напрямую изменять состояние, ты
          отправляешь "действия" (actions), а специальная функция – редьюсер
          (reducer) – определяет, как состояние должно обновиться.
        </p>
        <br></br>
        <pre className="pre-code">
          <code>
            {`
function counterReducer(state, action) {
  switch (action.type) {
    case "increment":
      return { count: state.count + 1 };
    case "decrement":
      return { count: state.count - 1 };
    default:
      return state;
  }
}

function Counter() {
  const [state, dispatch] = useReducer(counterReducer, { count: 0 });

  return (
    <div>
      <button onClick={() => dispatch({ type: "decrement" })}>-</button>
      <span>{state.count}</span>
      <button onClick={() => dispatch({ type: "increment" })}>+</button>
    </div>
  );
}

          `}
          </code>
        </pre>
        <br></br>
        <h2>Подводные камни / Важные моменты</h2>
        <p>Используем при:</p>
        <ul>
          <li>Если состояние сложное (объекты, массивы, много действий)</li>
          <li>Если логика изменения разветвлённая</li>
          <li>Если требуется централизованное управление состоянием</li>
        </ul>
        <a href="https://react.dev/reference/react/useReducer">Ссылка</a>
      </section>
      <section>
        <h2>useId</h2>
        <p>
          это встроенный хук, появившийся в React 18, который генерирует
          уникальные идентификаторы. Он особенно полезен для создания стабильных
          и уникальных id для элементов, например, при связывании label с input.
          Главное преимущество в том, что сгенерированные id будут одинаковыми
          на сервере и на клиенте, что помогает избежать проблем при гидратации
          (SSR).
        </p>
        <br></br>
        <pre className="pre-code">
          <code>
            {`
import React, { useId } from 'react';

function FormInput() {
  const id = useId();

  return (
    <div>
      <label htmlFor={id}>Ваше имя:</label>
      <input id={id} type="text" />
    </div>
  );
}

export default FormInput;
          `}
          </code>
        </pre>
        <br></br>
        <h2>Подводные камни / Важные моменты</h2>
        <ul>
          <li>
            <span className="bold">Не для ключей в списках:</span> Хотя useId
            генерирует уникальные строки, его не рекомендуется использовать для
            атрибута key в списках. Для ключей лучше использовать уникальные
            свойства элементов.
          </li>
          <li>
            <span className="bold">Использование для доступности:</span>
            Основное назначение useId — генерация id для компонентов, связанных
            с элементами управления формами, модальными окнами и другими
            интерактивными элементами. Это помогает корректно связывать элементы
            (например, label с input) и улучшает доступность приложения.
          </li>
          <li>
            <span className="bold">Комбинирование с другими хуками:</span>Если
            тебе нужно генерировать несколько id в одном компоненте, можно
            вызывать useId несколько раз или комбинировать его с другими
            значениями, чтобы создавать более сложные идентификаторы.
          </li>
          <li>
            <span className="bold">
              Стабильность между сервером и клиентом:
            </span>
            Благодаря тому, что useId работает одинаково при серверном и
            клиентском рендеринге, ты избегаешь ошибок, связанных с
            рассинхронизацией id, что особенно важно в приложениях с SSR. 4.
          </li>
        </ul>
        <a href="https://www.npmjs.com/package/react-hook-form">Ссылка</a>
      </section>
      <section>
        <h2>useTransition</h2>
        <p>
          это хук, позволяющий помечать обновления состояния как «переходы»
          (transitions). Это значит, что не срочные обновления можно выполнять с
          меньшим приоритетом, чтобы основной UI оставался отзывчивым
        </p>
        <br></br>
        <pre className="pre-code">
          <code>
            {`
import React, { useState, useTransition } from 'react';

function SearchableList({ items }) {
  const [query, setQuery] = useState('');
  const [filteredItems, setFilteredItems] = useState(items);
  const [isPending, startTransition] = useTransition();

  const handleInputChange = (e) => {
    const value = e.target.value;
    setQuery(value);

    // Обновление списка выполняется как переход, чтобы UI оставался отзывчивым
    startTransition(() => {
      const result = items.filter(item =>
        item.toLowerCase().includes(value.toLowerCase())
      );
      setFilteredItems(result);
    });
  };

  return (
    <div>
      <input
        type="text"
        value={query}
        onChange={handleInputChange}
        placeholder="Поиск..."
      />
      {isPending && <div>Загрузка...</div>}
      <ul>
        {filteredItems.map((item, index) => (
          <li key={index}>{item}</li>
        ))}
      </ul>
    </div>
  );
}

export default SearchableList;
  );
}

          `}
          </code>
        </pre>
        <br></br>
        <h2>Подводные камни / Важные моменты</h2>
        <ul>
          <li>
            <span className="bold">Совместимость:</span>useTransition доступен
            начиная с React 18, поэтому для его использования требуется
            обновлённая версия библиотеки.
          </li>
          <li>
            <span className="bold">Индикаторы загрузки:</span>Значение isPending
            удобно использовать для показа спиннеров или других визуальных
            индикаторов, чтобы пользователь понимал, что данные обновляются.
          </li>
          <li>
            <span className="bold">Оптимизация производительности:</span>Помечая
            обновления как переходы, ты помогаешь React распределить нагрузку и
            сохранить плавность интерфейса даже при выполнении сложных операций.
          </li>
        </ul>
        <a href="https://react.dev/reference/react/useTransition">Ссылка</a>
      </section>
      <section>
        <h2>useOptimistic</h2>
        <p>
          то экспериментальный хук, который предоставляет возможность обновлять
          состояние оптимистично. Его суть в том, что ты сразу меняешь
          отображаемое состояние, а затем асинхронно подтверждаешь или
          откатываешь его, если сервер сообщает об ошибке.
        </p>
        <br></br>
        <pre className="pre-code">
          <code>
            {`
import React from 'react';
import { useOptimistic } from 'react'; // Учти, что этот хук находится в экспериментальной версии React

function LikeButton() {
  // Инициализируем оптимистичное состояние лайков как 0
  const [likes, updateLikes] = useOptimistic(0, (state, action) => {
    switch (action.type) {
      case 'increment':
        return state + 1;
      case 'rollback':
        return action.payload; // возвращаем предыдущее значение
      default:
        return state;
    }
  });

  const handleLike = () => {
    const previousLikes = likes; // сохраняем текущее значение для возможного отката
    // Оптимистично увеличиваем лайки
    updateLikes({ type: 'increment' });

    // Имитация запроса к серверу
    fakeServerRequest()
      .then(() => {
        // Запрос успешен – оставляем обновлённое состояние
      })
      .catch(() => {
        // В случае ошибки откатываем состояние к предыдущему значению
        updateLikes({ type: 'rollback', payload: previousLikes });
      });
  };

  return (
    <div>
      <button onClick={handleLike}>👍 Like</button>
      <span> {likes} </span>
    </div>
  );
}

// Функция для имитации запроса к серверу
function fakeServerRequest() {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      // Случайный исход: 50% шанс успеха или ошибки
      Math.random() > 0.5 ? resolve() : reject();
    }, 1000);
  });
}

export default LikeButton;

          `}
          </code>
        </pre>
        <br></br>
        <h2>Подводные камни / Важные моменты</h2>
        <ul>
          <li>
            <span className="bold">Экспериментальность:</span>useOptimistic —
            экспериментальный хук, и его API может измениться в будущих версиях
            React. Используй его с осторожностью в продакшене.
          </li>
          <li>
            <span className="bold">Обработка ошибок:</span>Всегда предусмотри
            механизм отката, чтобы в случае ошибки серверного запроса состояние
            можно было вернуть к прежнему значению.
          </li>
          <li>
            <span className="bold">Комбинирование с другими хуками:</span>
            useOptimistic хорошо работает в связке с другими асинхронными хуками
            (например, useTransition) для создания отзывчивого интерфейса.
          </li>
        </ul>
        <a href="https://www.npmjs.com/package/react-hook-form">Ссылка</a>
      </section>
      <Buttons prev={"/builders"} next={"/react-hook-form"} />
    </div>
  );
}
